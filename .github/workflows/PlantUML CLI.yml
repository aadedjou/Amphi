name: generate plantuml
on: push
jobs:
  generate_uml:
    runs-on: ubuntu-latest
    name: plantuml
    steps:
    - name: PlantUML CLI
      with:
        args: conception/test.pu
      uses: lokkju/github-action-plantuml@v0.0.1